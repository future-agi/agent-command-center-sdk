# Copy to .env and fill in.
# .env is gitignored at the repo root.

AGENTCC_API_KEY=sk-...
AGENTCC_BASE_URL=https://gateway.futureagi.com

# Set to 1 to enable create/delete tests (auto-cleanup afterwards)
MUTATING=
